import random 

valor_random1: int = random.randint(1, 200)
valor_random2: int = random.randint(1, 200)

suma: int = valor_random1 + valor_random2

mensaje: str = "El resultado de la suma de () y () es: ()"
print(mensaje)